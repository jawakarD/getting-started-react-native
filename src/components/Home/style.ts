import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  view: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    margin: 50,
  },
  text: {
    fontSize: 20,
    marginBottom: 30,
    textAlign: 'center',
  },
});
